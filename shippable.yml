language: python

python:
  - "3.5"

install:
  - pip install pep8
  - pip install Django

before_script:
  - find ./ -name "*.py" | xargs pep8 --show-source --max-line-length=100 --ignore=W191
  - ./manage.py migrate

script:
  - ./manage.py test
